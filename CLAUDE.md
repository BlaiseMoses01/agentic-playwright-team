## Custom Docstrings on Reusable Test Code

bdd project run flags
`--project=desktop` runs tests in desktop chrome config
`--project=mobile` runs tests in mobile chrome config
`--project=ios` runs tests in ios safari mode mobile
